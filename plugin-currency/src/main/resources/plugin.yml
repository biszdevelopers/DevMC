name: Currency
version: 1.0.0-SNAPSHOT
main: dev.bisz.currency.CurrencyPlugin
api-version: "26.2"
authors: [BisZ]
depend: [Bundler]

permissions:
  currency.nits.debug:
    description: Allows the administrator-only /nits debug command.
    default: op
  currency.archive.debug:
    description: Allows manual currency transaction-cache archival.
    default: op
